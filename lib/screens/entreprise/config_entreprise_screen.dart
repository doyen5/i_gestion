import 'package:flutter/material.dart';

class ConfigEntrepriseScreen extends StatelessWidget {
  const ConfigEntrepriseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Configuration Entreprise')),
      body: Center(
        child: Text('Écran Configuration Entreprise - À implémenter'),
      ),
    );
  }
}
